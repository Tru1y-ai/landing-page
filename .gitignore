node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store

# .agents/skills/ IS committed — the skills are part of this project.
# .claude/skills/ is not: it is 21 symlinks holding absolute paths into one
# machine's home directory, regenerated by `npx skills add` on any checkout.
.claude/skills/
